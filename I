<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Nepal Flood Relief 2026 | Helping Hearts</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --primary: #e63946;
      --primary-hover: #d62828;
      --primary-glow: rgba(230, 57, 70, 0.25);
      --dark: #0f172a;
      --slate-800: #1e293b;
      --slate-700: #334155;
      --slate-100: #f1f5f9;
      --light: #f8fafc;
      --white: #ffffff;
      --text-muted: #64748b;
      --border-color: #e2e8f0;
      --shadow-sm: 0 1px 3px rgba(0,0,0,0.05);
      --shadow-md: 0 4px 6px -1px rgba(0,0,0,0.07), 0 2px 4px -2px rgba(0,0,0,0.05);
      --shadow-xl: 0 20px 25px -5px rgba(0,0,0,0.1), 0 8px 10px -6px rgba(0,0,0,0.05);
      --radius-sm: 8px;
      --radius-md: 12px;
      --radius-lg: 20px;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Plus Jakarta Sans', sans-serif;
      -webkit-tap-highlight-color: transparent;
    }

    body {
      background-color: var(--light);
      color: var(--dark);
      line-height: 1.6;
    }

    header {
      background: var(--white);
      border-bottom: 1px solid var(--border-color);
      position: sticky;
      top: 0;
      z-index: 100;
      box-shadow: var(--shadow-sm);
    }

    .navbar {
      max-width: 1200px;
      margin: 0 auto;
      padding: 1rem 1.5rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .brand {
      font-weight: 800;
      font-size: 1.35rem;
      color: var(--primary);
      display: flex;
      align-items: center;
      gap: 0.5rem;
      text-decoration: none;
    }

    .urgent-badge {
      background: #fee2e2;
      color: #991b1b;
      padding: 0.35rem 0.85rem;
      border-radius: 9999px;
      font-size: 0.8rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .hero-container {
      max-width: 1200px;
      margin: 2.5rem auto;
      padding: 0 1.5rem;
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap: 3rem;
      align-items: start;
    }

    .hero-content h1 {
      font-size: 2.6rem;
      line-height: 1.2;
      font-weight: 800;
      color: var(--dark);
      margin-bottom: 1rem;
    }

    .hero-content h1 span {
      color: var(--primary);
    }

    .hero-content p {
      font-size: 1.1rem;
      color: var(--text-muted);
      margin-bottom: 1.5rem;
    }

    .impact-stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      margin: 2rem 0;
    }

    .stat-card {
      background: var(--white);
      padding: 1.2rem;
      border-radius: var(--radius-md);
      border: 1px solid var(--border-color);
      box-shadow: var(--shadow-sm);
      text-align: center;
    }

    .stat-card .number {
      font-size: 1.6rem;
      font-weight: 800;
      color: var(--primary);
    }

    .stat-card .label {
      font-size: 0.85rem;
      color: var(--text-muted);
      font-weight: 500;
    }

    .progress-box {
      background: var(--white);
      padding: 1.5rem;
      border-radius: var(--radius-md);
      border: 1px solid var(--border-color);
      box-shadow: var(--shadow-sm);
      margin-bottom: 2rem;
    }

    .progress-meta {
      display: flex;
      justify-content: space-between;
      font-size: 0.95rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
    }

    .progress-bar-bg {
      background: #e2e8f0;
      height: 12px;
      border-radius: 9999px;
      overflow: hidden;
    }

    .progress-bar-fill {
      background: linear-gradient(90deg, #e63946, #ff758f);
      width: 68%;
      height: 100%;
      border-radius: 9999px;
    }

    .donation-card {
      background: var(--white);
      border-radius: var(--radius-lg);
      padding: 2rem;
      border: 1px solid var(--border-color);
      box-shadow: var(--shadow-xl);
      position: sticky;
      top: 5.5rem;
    }

    .card-header {
      margin-bottom: 1.5rem;
      text-align: center;
    }

    .card-header h2 {
      font-size: 1.6rem;
      font-weight: 700;
      color: var(--dark);
    }

    .card-header p {
      font-size: 0.9rem;
      color: var(--text-muted);
    }

    .amount-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 0.75rem;
      margin-bottom: 1.25rem;
    }

    .amount-btn {
      background: var(--light);
      border: 1.5px solid var(--border-color);
      padding: 0.85rem 0.5rem;
      border-radius: var(--radius-sm);
      font-weight: 700;
      font-size: 1.05rem;
      color: var(--dark);
      cursor: pointer;
      transition: all 0.2s ease;
    }

    .amount-btn.active, .amount-btn:hover {
      border-color: var(--primary);
      background: #fff5f5;
      color: var(--primary);
    }

    .custom-amount-wrapper {
      position: relative;
      margin-bottom: 1.25rem;
    }

    .currency-symbol {
      position: absolute;
      left: 1rem;
      top: 50%;
      transform: translateY(-50%);
      font-weight: 700;
      color: var(--text-muted);
    }

    .custom-input {
      width: 100%;
      padding: 0.85rem 1rem 0.85rem 2.2rem;
      border: 1.5px solid var(--border-color);
      border-radius: var(--radius-sm);
      font-size: 1rem;
      font-weight: 600;
      outline: none;
      transition: border-color 0.2s ease;
    }

    .custom-input:focus {
      border-color: var(--primary);
    }

    .donor-info-group {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
      margin-bottom: 1.25rem;
    }

    .donor-input {
      width: 100%;
      padding: 0.75rem 1rem;
      border: 1.5px solid var(--border-color);
      border-radius: var(--radius-sm);
      font-size: 0.95rem;
      outline: none;
      transition: border-color 0.2s;
    }

    .donor-input:focus {
      border-color: var(--primary);
    }

    .pay-btn {
      width: 100%;
      background: var(--primary);
      color: var(--white);
      border: none;
      padding: 1rem;
      border-radius: var(--radius-sm);
      font-size: 1.1rem;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 4px 14px var(--primary-glow);
      transition: all 0.2s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }

    .pay-btn:hover {
      background: var(--primary-hover);
      transform: translateY(-1px);
    }

    .qr-toggle-btn {
      width: 100%;
      background: transparent;
      border: 1px dashed var(--slate-700);
      color: var(--slate-700);
      padding: 0.65rem;
      border-radius: var(--radius-sm);
      margin-top: 0.75rem;
      font-size: 0.85rem;
      font-weight: 600;
      cursor: pointer;
    }

    .qr-section {
      display: none;
      text-align: center;
      margin-top: 1.25rem;
      padding-top: 1.25rem;
      border-top: 1px dashed var(--border-color);
    }

    .qr-section.show {
      display: block;
    }

    .qr-box {
      width: 160px;
      height: 160px;
      margin: 0 auto 0.75rem;
      border: 1px solid var(--border-color);
      border-radius: var(--radius-sm);
      padding: 8px;
      background: var(--white);
    }

    .qr-box img {
      width: 100%;
      height: 100%;
      object-fit: contain;
    }

    .upi-id-badge {
      display: inline-block;
      background: var(--slate-100);
      padding: 0.35rem 0.75rem;
      border-radius: var(--radius-sm);
      font-size: 0.85rem;
      font-weight: 600;
      color: var(--slate-800);
    }

    footer {
      text-align: center;
      padding: 2rem 1.5rem;
      color: var(--text-muted);
      font-size: 0.85rem;
      border-top: 1px solid var(--border-color);
      margin-top: 4rem;
    }

    @media (max-width: 900px) {
      .hero-container {
        grid-template-columns: 1fr;
        gap: 2rem;
      }
      .hero-content h1 {
        font-size: 2.1rem;
      }
      .donation-card {
        position: static;
      }
    }
  </style>
</head>
<body>

  <header>
    <div class="navbar">
      <a href="#" class="brand">
        ❤️ Helping Hearts
      </a>
      <span class="urgent-badge">Emergency Relief</span>
    </div>
  </header>

  <main class="hero-container">
    <section class="hero-content">
      <h1>Emergency Relief For <span>Nepal Floods</span></h1>
      <p>
        Unprecedented flash floods and landslides have swept through communities across Nepal, displacing thousands of families. Your immediate support delivers clean water, emergency ration kits, medical supplies, and temporary shelters.
      </p>

      <div class="progress-box">
        <div class="progress-meta">
          <span>Raised: ₹6,80,000</span>
          <span>Target: ₹10,00,000</span>
        </div>
        <div class="progress-bar-bg">
          <div class="progress-bar-fill"></div>
        </div>
      </div>

      <div class="impact-stats">
        <div class="stat-card">
          <div class="number">12,000+</div>
          <div class="label">People Affected</div>
        </div>
        <div class="stat-card">
          <div class="number">1,450+</div>
          <div class="label">Families Relocated</div>
        </div>
        <div class="stat-card">
          <div class="number">100%</div>
          <div class="label">Direct Support</div>
        </div>
      </div>
    </section>

    <aside class="donation-card">
      <div class="card-header">
        <h2>Donate Securely</h2>
        <p>Direct UPI transfer • Zero processing fee</p>
      </div>

      <div class="amount-grid">
        <button type="button" class="amount-btn active" data-val="500">₹500</button>
        <button type="button" class="amount-btn" data-val="1000">₹1,000</button>
        <button type="button" class="amount-btn" data-val="2500">₹2,500</button>
      </div>

      <div class="custom-amount-wrapper">
        <span class="currency-symbol">₹</span>
        <input type="number" id="customAmount" class="custom-input" placeholder="Enter custom amount" value="500" min="10" />
      </div>

      <div class="donor-info-group">
        <input type="text" id="donorName" class="donor-input" placeholder="Your Name (Optional)" />
        <input type="tel" id="donorPhone" class="donor-input" placeholder="Phone / Email (For Receipt)" />
      </div>

      <button id="payNowBtn" class="pay-btn">
        Pay via UPI Apps
      </button>

      <button id="toggleQrBtn" type="button" class="qr-toggle-btn">
        Scan QR Code Instead
      </button>

      <div id="qrSection" class="qr-section">
        <div class="qr-box">
          <img id="dynamicQr" src="" alt="Scan & Pay QR" />
        </div>
        <div class="upi-id-badge">
          UPI ID: <span id="displayUpi">helpinghearts@upi</span>
        </div>
      </div>
    </aside>
  </main>

  <footer>
    <p>© 2026 Helping Hearts Foundation (helpinghearts4u.online). All relief funds are directly deployed for ground operations.</p>
  </footer>

  <script>
    const UPI_CONFIG = {
      upiId: "helpinghearts@upi", // Replace with your actual UPI ID
      payeeName: "Helping Hearts Relief Fund",
      merchantCode: "0000"
    };

    let selectedAmount = 500;

    const amountBtns = document.querySelectorAll(".amount-btn");
    const customInput = document.getElementById("customAmount");
    const payBtn = document.getElementById("payNowBtn");
    const toggleQrBtn = document.getElementById("toggleQrBtn");
    const qrSection = document.getElementById("qrSection");
    const dynamicQr = document.getElementById("dynamicQr");
    const displayUpi = document.getElementById("displayUpi");

    displayUpi.textContent = UPI_CONFIG.upiId;

    amountBtns.forEach(btn => {
      btn.addEventListener("click", () => {
        amountBtns.forEach(b => b.classList.remove("active"));
        btn.classList.add("active");
        selectedAmount = parseInt(btn.dataset.val, 10);
        customInput.value = selectedAmount;
        updateQrCode();
      });
    });

    customInput.addEventListener("input", (e) => {
      const val = parseInt(e.target.value, 10);
      selectedAmount = isNaN(val) || val <= 0 ? 0 : val;
      amountBtns.forEach(btn => {
        btn.classList.toggle("active", parseInt(btn.dataset.val, 10) === selectedAmount);
      });
      updateQrCode();
    });

    function generateTransactionRef() {
      return "HH" + Date.now();
    }

    function buildUpiUrl(amount) {
      const txnRef = generateTransactionRef();
      const params = new URLSearchParams({
        pa: UPI_CONFIG.upiId,
        pn: UPI_CONFIG.payeeName,
        am: amount.toFixed(2),
        cu: "INR",
        tn: `Nepal Relief ${txnRef}`,
        tr: txnRef
      });
      return `upi://pay?${params.toString()}`;
    }

    function updateQrCode() {
      const amt = selectedAmount > 0 ? selectedAmount : 500;
      const upiUrl = buildUpiUrl(amt);
      dynamicQr.src = `https://api.qrserver.com/v1/create-qr-code/?size=180x180&data=${encodeURIComponent(upiUrl)}`;
    }

    payBtn.addEventListener("click", () => {
      if (!selectedAmount || selectedAmount <= 0) {
        alert("Please enter a valid donation amount.");
        return;
      }
      const intentUrl = buildUpiUrl(selectedAmount);
      window.location.href = intentUrl;
    });

    toggleQrBtn.addEventListener("click", () => {
      qrSection.classList.toggle("show");
      if (qrSection.classList.contains("show")) {
        updateQrCode();
      }
    });

    updateQrCode();
  </script>
</body>
</html>
